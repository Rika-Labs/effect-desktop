# Issue 769 PR

PR: https://github.com/Rika-Labs/effect-desktop/pull/796

Summary:

- Adds an explicit installable CLI artifact boundary for consumers outside the monorepo.
- Keeps the workspace manifest linked with `workspace:*` while rewriting only the copied artifact manifest to sibling `file:` specs.
- Proves the artifact from a temp consumer app with `bun install` and `bunx desktop`.

Verification before PR:

- `bun install --frozen-lockfile` — passed.
- `bun test scripts/pack-installable-cli.test.ts tests/repo-shape.test.ts` — passed.
- `bun run check` — passed.
- `bun run typecheck` — passed.
- `bun run lint` — passed.
- `bun run lint:types` — passed.
- Changed-file Prettier check — passed.
- `bun test` — passed.

Known local-only gap: full `bun run format:check` is blocked by existing `.devin/config.local.json` formatting drift outside this change set.
