# Issue 815 PR

PR: https://github.com/Rika-Labs/effect-desktop/pull/818

Summary:

- Adds one Dialog display-text schema for platform-visible native dialog strings.
- Rejects empty provided values and ASCII control bytes before bridge transport.
- Preserves omitted optional fields as the platform-default path.

Verification before PR:

- `bun test packages/native/src/index.test.ts` — passed.
- `bun run typecheck` — passed.
- `bun run lint:types` — passed.
- Changed-file Prettier check — passed.
- `bun run check` — passed.
- `bun run lint` — passed.
- `bun test packages/native/src/index.test.ts tests/repo-shape.test.ts` — passed.
- `bun test` — passed.
