# Issue 812 PR: Validate CrashReporter.flush Output Counts

## Pull Request

https://github.com/Rika-Labs/effect-desktop/pull/835

## Summary

`CrashReporter.flush` now decodes host output with a non-negative integer count schema. The bridge tests reject negative, fractional, `NaN`, and infinite counts as `InvalidOutput`, while valid zero and positive counts still decode.

Handoff: `/code-review`
