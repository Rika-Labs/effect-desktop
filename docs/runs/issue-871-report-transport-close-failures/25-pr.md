## PR

- https://github.com/Rika-Labs/effect-desktop/pull/878

## CI

- Current run passed on ubuntu, Windows, and macOS.
- Older superseded runs remained visible as failed/cancelled in `gh pr checks`, but the watched command exited cleanly after the newer run passed.

Handoff: `/code-review`
