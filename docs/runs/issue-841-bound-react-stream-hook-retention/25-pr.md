# Issue 841 PR: Bound React Stream Hook Retention

## Pull Request

https://github.com/Rika-Labs/effect-desktop/pull/844

## Summary

`useDesktopStream` now retains a bounded number of emitted items by default and exposes an explicit retention policy. Callers can set `capacity` or use `capacity: 0` with `onItem` for callback-only consumption.

Handoff: `/code-review`
