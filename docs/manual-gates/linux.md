# Linux Manual Gates

Release-cycle sign-off file for matrix cells that cannot run on regular CI.

Tracked gates:

| Gate                          | Cell        | Status                   | Evidence                                                                         |
| ----------------------------- | ----------- | ------------------------ | -------------------------------------------------------------------------------- |
| `C.81` open-at-login contract | `linux-x64` | pending release sign-off | Manual Linux release-gate evidence required where a logged-in session is needed. |
