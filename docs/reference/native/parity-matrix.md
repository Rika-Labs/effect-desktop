---
title: Native parity matrix
description: Generated TypeScript native surface to Rust host dispatch registry parity.
kind: reference
audience: maintainers
effect_version: 4
---

# Native parity matrix

This page is generated by `bun scripts/generate-native-parity-matrix.ts` from `Native.all` schema docs and the Rust host dispatch registry.

- Total declared native methods: 232
- Routed by the Rust host: 208
- Missing from the Rust host dispatch registry: 24

The machine-readable matrix lives in [`parity-matrix.json`](parity-matrix.json).

| Surface                     | Method                      | Support       | Host method                               | Host router |
| --------------------------- | --------------------------- | ------------- | ----------------------------------------- | ----------- |
| `ActivationRegistry`        | `isSupported`               | `supported`   | `ActivationRegistry.isSupported`          | `routed`    |
| `ActivationRegistry`        | `listSurfaces`              | `supported`   | `ActivationRegistry.listSurfaces`         | `routed`    |
| `ActivationRegistry`        | `registerSurface`           | `supported`   | `ActivationRegistry.registerSurface`      | `routed`    |
| `ActivationRegistry`        | `unregisterSurface`         | `supported`   | `ActivationRegistry.unregisterSurface`    | `routed`    |
| `App`                       | `focus`                     | `unsupported` | `App.focus`                               | `routed`    |
| `App`                       | `quit`                      | `unsupported` | `App.quit`                                | `routed`    |
| `App`                       | `requestSingleInstanceLock` | `unsupported` | `App.requestSingleInstanceLock`           | `routed`    |
| `App`                       | `restart`                   | `unsupported` | `App.restart`                             | `routed`    |
| `AppMetadata`               | `getInfo`                   | `supported`   | `AppMetadata.getInfo`                     | `routed`    |
| `AppMetadata`               | `getLaunchContext`          | `supported`   | `AppMetadata.getLaunchContext`            | `routed`    |
| `AppMetadata`               | `getPaths`                  | `supported`   | `AppMetadata.getPaths`                    | `routed`    |
| `Association`               | `getFileAssociations`       | `unsupported` | `Association.getFileAssociations`         | `routed`    |
| `Association`               | `isDefaultProtocolClient`   | `unsupported` | `Association.isDefaultProtocolClient`     | `routed`    |
| `Association`               | `setDefaultProtocolClient`  | `unsupported` | `Association.setDefaultProtocolClient`    | `routed`    |
| `AttachmentIntake`          | `dispose`                   | `supported`   | `AttachmentIntake.dispose`                | `routed`    |
| `AttachmentIntake`          | `ingest`                    | `supported`   | `AttachmentIntake.ingest`                 | `routed`    |
| `AttachmentIntake`          | `inspect`                   | `supported`   | `AttachmentIntake.inspect`                | `routed`    |
| `AttachmentIntake`          | `isSupported`               | `supported`   | `AttachmentIntake.isSupported`            | `routed`    |
| `Autostart`                 | `disable`                   | `unsupported` | `Autostart.disable`                       | `routed`    |
| `Autostart`                 | `enable`                    | `unsupported` | `Autostart.enable`                        | `routed`    |
| `Autostart`                 | `isEnabled`                 | `unsupported` | `Autostart.isEnabled`                     | `routed`    |
| `Clipboard`                 | `clear`                     | `unsupported` | `Clipboard.clear`                         | `routed`    |
| `Clipboard`                 | `isSupported`               | `supported`   | `Clipboard.isSupported`                   | `routed`    |
| `Clipboard`                 | `readHtml`                  | `unsupported` | `Clipboard.readHtml`                      | `routed`    |
| `Clipboard`                 | `readImage`                 | `unsupported` | `Clipboard.readImage`                     | `routed`    |
| `Clipboard`                 | `readText`                  | `unsupported` | `Clipboard.readText`                      | `routed`    |
| `Clipboard`                 | `writeHtml`                 | `unsupported` | `Clipboard.writeHtml`                     | `routed`    |
| `Clipboard`                 | `writeImage`                | `unsupported` | `Clipboard.writeImage`                    | `routed`    |
| `Clipboard`                 | `writeText`                 | `unsupported` | `Clipboard.writeText`                     | `routed`    |
| `ContextMenu`               | `bindCommand`               | `unsupported` | `ContextMenu.bindCommand`                 | `missing`   |
| `ContextMenu`               | `buildFromTemplate`         | `unsupported` | `ContextMenu.buildFromTemplate`           | `missing`   |
| `ContextMenu`               | `show`                      | `unsupported` | `ContextMenu.show`                        | `missing`   |
| `CrashReporter`             | `flush`                     | `unsupported` | `CrashReporter.flush`                     | `routed`    |
| `CrashReporter`             | `getReports`                | `unsupported` | `CrashReporter.getReports`                | `routed`    |
| `CrashReporter`             | `recordBreadcrumb`          | `unsupported` | `CrashReporter.recordBreadcrumb`          | `routed`    |
| `CrashReporter`             | `start`                     | `unsupported` | `CrashReporter.start`                     | `routed`    |
| `DiagnosticsBundle`         | `collect`                   | `supported`   | `DiagnosticsBundle.collect`               | `routed`    |
| `DiagnosticsBundle`         | `isSupported`               | `supported`   | `DiagnosticsBundle.isSupported`           | `routed`    |
| `DiagnosticsBundle`         | `redact`                    | `supported`   | `DiagnosticsBundle.redact`                | `routed`    |
| `DiagnosticsBundle`         | `write`                     | `supported`   | `DiagnosticsBundle.write`                 | `routed`    |
| `Dialog`                    | `confirm`                   | `supported`   | `Dialog.confirm`                          | `routed`    |
| `Dialog`                    | `message`                   | `supported`   | `Dialog.message`                          | `routed`    |
| `Dialog`                    | `openDirectory`             | `partial`     | `Dialog.openDirectory`                    | `routed`    |
| `Dialog`                    | `openFile`                  | `partial`     | `Dialog.openFile`                         | `routed`    |
| `Dialog`                    | `saveFile`                  | `supported`   | `Dialog.saveFile`                         | `routed`    |
| `DisplayCapture`            | `captureDisplay`            | `partial`     | `DisplayCapture.captureDisplay`           | `routed`    |
| `DisplayCapture`            | `captureRegion`             | `partial`     | `DisplayCapture.captureRegion`            | `routed`    |
| `DisplayCapture`            | `captureWindow`             | `partial`     | `DisplayCapture.captureWindow`            | `routed`    |
| `DisplayCapture`            | `isSupported`               | `supported`   | `DisplayCapture.isSupported`              | `routed`    |
| `DistributionParity`        | `isSupported`               | `supported`   | `DistributionParity.isSupported`          | `routed`    |
| `DistributionParity`        | `verify`                    | `supported`   | `DistributionParity.verify`               | `routed`    |
| `Dock`                      | `isSupported`               | `supported`   | `Dock.isSupported`                        | `routed`    |
| `Dock`                      | `requestAttention`          | `supported`   | `Dock.requestAttention`                   | `routed`    |
| `Dock`                      | `setBadgeCount`             | `partial`     | `Dock.setBadgeCount`                      | `routed`    |
| `Dock`                      | `setBadgeText`              | `partial`     | `Dock.setBadgeText`                       | `routed`    |
| `Dock`                      | `setJumpList`               | `unsupported` | `Dock.setJumpList`                        | `missing`   |
| `Dock`                      | `setMenu`                   | `unsupported` | `Dock.setMenu`                            | `routed`    |
| `Dock`                      | `setProgress`               | `unsupported` | `Dock.setProgress`                        | `missing`   |
| `EgressPolicy`              | `decide`                    | `partial`     | `EgressPolicy.decide`                     | `routed`    |
| `EgressPolicy`              | `isSupported`               | `partial`     | `EgressPolicy.isSupported`                | `routed`    |
| `EgressPolicy`              | `record`                    | `partial`     | `EgressPolicy.record`                     | `routed`    |
| `ExecutionSandbox`          | `create`                    | `unsupported` | `ExecutionSandbox.create`                 | `routed`    |
| `ExecutionSandbox`          | `destroy`                   | `unsupported` | `ExecutionSandbox.destroy`                | `routed`    |
| `ExecutionSandbox`          | `isSupported`               | `supported`   | `ExecutionSandbox.isSupported`            | `routed`    |
| `ExecutionSandbox`          | `run`                       | `unsupported` | `ExecutionSandbox.run`                    | `routed`    |
| `ExtensionConfig`           | `isSupported`               | `supported`   | `ExtensionConfig.isSupported`             | `routed`    |
| `ExtensionConfig`           | `read`                      | `supported`   | `ExtensionConfig.read`                    | `routed`    |
| `ExtensionConfig`           | `redact`                    | `supported`   | `ExtensionConfig.redact`                  | `routed`    |
| `ExtensionConfig`           | `reset`                     | `supported`   | `ExtensionConfig.reset`                   | `routed`    |
| `ExtensionConfig`           | `write`                     | `supported`   | `ExtensionConfig.write`                   | `routed`    |
| `ExtensionPackage`          | `install`                   | `supported`   | `ExtensionPackage.install`                | `routed`    |
| `ExtensionPackage`          | `isSupported`               | `supported`   | `ExtensionPackage.isSupported`            | `routed`    |
| `ExtensionPackage`          | `list`                      | `supported`   | `ExtensionPackage.list`                   | `routed`    |
| `ExtensionPackage`          | `remove`                    | `supported`   | `ExtensionPackage.remove`                 | `routed`    |
| `ExtensionPackage`          | `update`                    | `supported`   | `ExtensionPackage.update`                 | `routed`    |
| `FocusedApplicationContext` | `isSupported`               | `supported`   | `FocusedApplicationContext.isSupported`   | `routed`    |
| `FocusedApplicationContext` | `snapshot`                  | `partial`     | `FocusedApplicationContext.snapshot`      | `routed`    |
| `FocusedApplicationContext` | `stopWatching`              | `unsupported` | `FocusedApplicationContext.stopWatching`  | `routed`    |
| `FocusedApplicationContext` | `watch`                     | `unsupported` | `FocusedApplicationContext.watch`         | `routed`    |
| `GlobalShortcut`            | `isRegistered`              | `supported`   | `GlobalShortcut.isRegistered`             | `routed`    |
| `GlobalShortcut`            | `isSupported`               | `supported`   | `GlobalShortcut.isSupported`              | `routed`    |
| `GlobalShortcut`            | `register`                  | `unsupported` | `GlobalShortcut.register`                 | `routed`    |
| `GlobalShortcut`            | `unregister`                | `unsupported` | `GlobalShortcut.unregister`               | `routed`    |
| `GlobalShortcut`            | `unregisterAll`             | `unsupported` | `GlobalShortcut.unregisterAll`            | `routed`    |
| `Job`                       | `fail`                      | `supported`   | `Job.fail`                                | `routed`    |
| `Job`                       | `get`                       | `supported`   | `Job.get`                                 | `routed`    |
| `Job`                       | `interrupt`                 | `supported`   | `Job.interrupt`                           | `routed`    |
| `Job`                       | `isSupported`               | `supported`   | `Job.isSupported`                         | `routed`    |
| `Job`                       | `pause`                     | `supported`   | `Job.pause`                               | `routed`    |
| `Job`                       | `reportProgress`            | `supported`   | `Job.reportProgress`                      | `routed`    |
| `Job`                       | `resume`                    | `supported`   | `Job.resume`                              | `routed`    |
| `Job`                       | `retry`                     | `supported`   | `Job.retry`                               | `routed`    |
| `Job`                       | `start`                     | `supported`   | `Job.start`                               | `routed`    |
| `Job`                       | `succeed`                   | `supported`   | `Job.succeed`                             | `routed`    |
| `LocalToolRuntime`          | `health`                    | `supported`   | `LocalToolRuntime.health`                 | `routed`    |
| `LocalToolRuntime`          | `isSupported`               | `supported`   | `LocalToolRuntime.isSupported`            | `routed`    |
| `LocalToolRuntime`          | `register`                  | `supported`   | `LocalToolRuntime.register`               | `routed`    |
| `LocalToolRuntime`          | `run`                       | `supported`   | `LocalToolRuntime.run`                    | `routed`    |
| `LocalToolRuntime`          | `stop`                      | `supported`   | `LocalToolRuntime.stop`                   | `routed`    |
| `Menu`                      | `bindCommand`               | `unsupported` | `Menu.bindCommand`                        | `missing`   |
| `Menu`                      | `capability`                | `unsupported` | `Menu.capability`                         | `missing`   |
| `Menu`                      | `clear`                     | `unsupported` | `Menu.clear`                              | `missing`   |
| `Menu`                      | `setApplicationMenu`        | `supported`   | `Menu.setApplicationMenu`                 | `routed`    |
| `Menu`                      | `setWindowMenu`             | `supported`   | `Menu.setWindowMenu`                      | `routed`    |
| `NativeFileSystem`          | `isSupported`               | `unsupported` | `NativeFileSystem.isSupported`            | `routed`    |
| `NativeFileSystem`          | `open`                      | `unsupported` | `NativeFileSystem.open`                   | `routed`    |
| `NativeFileSystem`          | `stat`                      | `unsupported` | `NativeFileSystem.stat`                   | `routed`    |
| `NativeFileSystem`          | `stopWatching`              | `unsupported` | `NativeFileSystem.stopWatching`           | `routed`    |
| `NativeFileSystem`          | `watch`                     | `unsupported` | `NativeFileSystem.watch`                  | `routed`    |
| `Notification`              | `close`                     | `partial`     | `Notification.close`                      | `routed`    |
| `Notification`              | `getPermissionStatus`       | `partial`     | `Notification.getPermissionStatus`        | `routed`    |
| `Notification`              | `isSupported`               | `supported`   | `Notification.isSupported`                | `routed`    |
| `Notification`              | `requestPermission`         | `partial`     | `Notification.requestPermission`          | `routed`    |
| `Notification`              | `show`                      | `partial`     | `Notification.show`                       | `routed`    |
| `Path`                      | `appData`                   | `supported`   | `Path.appData`                            | `routed`    |
| `Path`                      | `cache`                     | `supported`   | `Path.cache`                              | `routed`    |
| `Path`                      | `downloads`                 | `supported`   | `Path.downloads`                          | `routed`    |
| `Path`                      | `home`                      | `supported`   | `Path.home`                               | `routed`    |
| `Path`                      | `logs`                      | `supported`   | `Path.logs`                               | `routed`    |
| `Path`                      | `temp`                      | `supported`   | `Path.temp`                               | `routed`    |
| `PowerMonitor`              | `isSupported`               | `unsupported` | `PowerMonitor.isSupported`                | `routed`    |
| `Protocol`                  | `deny`                      | `supported`   | `Protocol.deny`                           | `routed`    |
| `Protocol`                  | `registerAppProtocol`       | `supported`   | `Protocol.registerAppProtocol`            | `routed`    |
| `Protocol`                  | `serveAsset`                | `supported`   | `Protocol.serveAsset`                     | `routed`    |
| `Protocol`                  | `serveRoute`                | `supported`   | `Protocol.serveRoute`                     | `routed`    |
| `RealtimeMediaSession`      | `close`                     | `partial`     | `RealtimeMediaSession.close`              | `routed`    |
| `RealtimeMediaSession`      | `interrupt`                 | `partial`     | `RealtimeMediaSession.interrupt`          | `routed`    |
| `RealtimeMediaSession`      | `isSupported`               | `supported`   | `RealtimeMediaSession.isSupported`        | `routed`    |
| `RealtimeMediaSession`      | `open`                      | `partial`     | `RealtimeMediaSession.open`               | `routed`    |
| `RealtimeMediaSession`      | `selectDevice`              | `partial`     | `RealtimeMediaSession.selectDevice`       | `routed`    |
| `RecentDocuments`           | `add`                       | `partial`     | `RecentDocuments.add`                     | `routed`    |
| `RecentDocuments`           | `clear`                     | `partial`     | `RecentDocuments.clear`                   | `routed`    |
| `RecentDocuments`           | `list`                      | `partial`     | `RecentDocuments.list`                    | `routed`    |
| `ResidentLifecycle`         | `disable`                   | `unsupported` | `ResidentLifecycle.disable`               | `routed`    |
| `ResidentLifecycle`         | `enable`                    | `unsupported` | `ResidentLifecycle.enable`                | `routed`    |
| `ResidentLifecycle`         | `getState`                  | `unsupported` | `ResidentLifecycle.getState`              | `routed`    |
| `ResidentLifecycle`         | `isSupported`               | `supported`   | `ResidentLifecycle.isSupported`           | `routed`    |
| `SafeStorage`               | `delete`                    | `unsupported` | `SafeStorage.delete`                      | `missing`   |
| `SafeStorage`               | `get`                       | `unsupported` | `SafeStorage.get`                         | `missing`   |
| `SafeStorage`               | `isAvailable`               | `supported`   | `SafeStorage.isAvailable`                 | `routed`    |
| `SafeStorage`               | `list`                      | `unsupported` | `SafeStorage.list`                        | `missing`   |
| `SafeStorage`               | `set`                       | `unsupported` | `SafeStorage.set`                         | `missing`   |
| `ScopedAccessGrant`         | `grant`                     | `unsupported` | `ScopedAccessGrant.grant`                 | `routed`    |
| `ScopedAccessGrant`         | `isSupported`               | `supported`   | `ScopedAccessGrant.isSupported`           | `routed`    |
| `ScopedAccessGrant`         | `resolve`                   | `unsupported` | `ScopedAccessGrant.resolve`               | `routed`    |
| `ScopedAccessGrant`         | `revoke`                    | `unsupported` | `ScopedAccessGrant.revoke`                | `routed`    |
| `Screen`                    | `getDisplays`               | `supported`   | `Screen.getDisplays`                      | `routed`    |
| `Screen`                    | `getPointerPoint`           | `supported`   | `Screen.getPointerPoint`                  | `routed`    |
| `Screen`                    | `getPrimaryDisplay`         | `supported`   | `Screen.getPrimaryDisplay`                | `routed`    |
| `Screen`                    | `isSupported`               | `supported`   | `Screen.isSupported`                      | `routed`    |
| `SelectionContext`          | `isSupported`               | `supported`   | `SelectionContext.isSupported`            | `routed`    |
| `SelectionContext`          | `readDocumentContext`       | `unsupported` | `SelectionContext.readDocumentContext`    | `routed`    |
| `SelectionContext`          | `readSelection`             | `unsupported` | `SelectionContext.readSelection`          | `routed`    |
| `SelectionContext`          | `stopWatching`              | `unsupported` | `SelectionContext.stopWatching`           | `routed`    |
| `SelectionContext`          | `watchFocus`                | `unsupported` | `SelectionContext.watchFocus`             | `routed`    |
| `Shell`                     | `openExternal`              | `supported`   | `Shell.openExternal`                      | `routed`    |
| `Shell`                     | `openPath`                  | `supported`   | `Shell.openPath`                          | `routed`    |
| `Shell`                     | `showItemInFolder`          | `supported`   | `Shell.showItemInFolder`                  | `routed`    |
| `Shell`                     | `trashItem`                 | `partial`     | `Shell.trashItem`                         | `routed`    |
| `SystemAppearance`          | `getAccentColor`            | `partial`     | `SystemAppearance.getAccentColor`         | `routed`    |
| `SystemAppearance`          | `getAppearance`             | `partial`     | `SystemAppearance.getAppearance`          | `routed`    |
| `SystemAppearance`          | `getReducedMotion`          | `partial`     | `SystemAppearance.getReducedMotion`       | `routed`    |
| `SystemAppearance`          | `getReducedTransparency`    | `partial`     | `SystemAppearance.getReducedTransparency` | `routed`    |
| `SystemAppearance`          | `isSupported`               | `supported`   | `SystemAppearance.isSupported`            | `routed`    |
| `TransactionalFileMutation` | `commit`                    | `supported`   | `TransactionalFileMutation.commit`        | `routed`    |
| `TransactionalFileMutation` | `isSupported`               | `supported`   | `TransactionalFileMutation.isSupported`   | `routed`    |
| `TransactionalFileMutation` | `prepare`                   | `supported`   | `TransactionalFileMutation.prepare`       | `routed`    |
| `TransactionalFileMutation` | `rollback`                  | `supported`   | `TransactionalFileMutation.rollback`      | `routed`    |
| `TransientWindowRole`       | `dismiss`                   | `unsupported` | `TransientWindowRole.dismiss`             | `routed`    |
| `TransientWindowRole`       | `isSupported`               | `supported`   | `TransientWindowRole.isSupported`         | `routed`    |
| `TransientWindowRole`       | `open`                      | `unsupported` | `TransientWindowRole.open`                | `routed`    |
| `TransientWindowRole`       | `reposition`                | `unsupported` | `TransientWindowRole.reposition`          | `routed`    |
| `Tray`                      | `create`                    | `partial`     | `Tray.create`                             | `routed`    |
| `Tray`                      | `destroy`                   | `partial`     | `Tray.destroy`                            | `routed`    |
| `Tray`                      | `isSupported`               | `supported`   | `Tray.isSupported`                        | `routed`    |
| `Tray`                      | `setIcon`                   | `partial`     | `Tray.setIcon`                            | `routed`    |
| `Tray`                      | `setMenu`                   | `partial`     | `Tray.setMenu`                            | `routed`    |
| `Tray`                      | `setTitle`                  | `partial`     | `Tray.setTitle`                           | `routed`    |
| `Tray`                      | `setTooltip`                | `partial`     | `Tray.setTooltip`                         | `routed`    |
| `Updater`                   | `check`                     | `unsupported` | `Updater.check`                           | `routed`    |
| `Updater`                   | `download`                  | `unsupported` | `Updater.download`                        | `routed`    |
| `Updater`                   | `getStatus`                 | `unsupported` | `Updater.getStatus`                       | `routed`    |
| `Updater`                   | `install`                   | `unsupported` | `Updater.install`                         | `routed`    |
| `Updater`                   | `installAndRestart`         | `unsupported` | `Updater.installAndRestart`               | `routed`    |
| `Updater`                   | `readyForRestart`           | `unsupported` | `Updater.readyForRestart`                 | `routed`    |
| `WebView`                   | `capability`                | `unsupported` | `WebView.capability`                      | `missing`   |
| `WebView`                   | `captureScreenshot`         | `unsupported` | `WebView.captureScreenshot`               | `missing`   |
| `WebView`                   | `create`                    | `unsupported` | `WebView.create`                          | `missing`   |
| `WebView`                   | `destroy`                   | `unsupported` | `WebView.destroy`                         | `missing`   |
| `WebView`                   | `getNavigationState`        | `unsupported` | `WebView.getNavigationState`              | `missing`   |
| `WebView`                   | `goBack`                    | `unsupported` | `WebView.goBack`                          | `missing`   |
| `WebView`                   | `goForward`                 | `unsupported` | `WebView.goForward`                       | `missing`   |
| `WebView`                   | `loadRoute`                 | `unsupported` | `WebView.loadRoute`                       | `missing`   |
| `WebView`                   | `loadUrl`                   | `unsupported` | `WebView.loadUrl`                         | `missing`   |
| `WebView`                   | `reload`                    | `unsupported` | `WebView.reload`                          | `missing`   |
| `WebView`                   | `setNavigationPolicy`       | `unsupported` | `WebView.setNavigationPolicy`             | `missing`   |
| `WebView`                   | `stop`                      | `unsupported` | `WebView.stop`                            | `missing`   |
| `Window`                    | `cancelAttention`           | `supported`   | `Window.cancelAttention`                  | `routed`    |
| `Window`                    | `center`                    | `supported`   | `Window.center`                           | `routed`    |
| `Window`                    | `centerOnDisplay`           | `supported`   | `Window.centerOnDisplay`                  | `routed`    |
| `Window`                    | `close`                     | `supported`   | `Window.destroy`                          | `routed`    |
| `Window`                    | `create`                    | `supported`   | `Window.create`                           | `routed`    |
| `Window`                    | `destroy`                   | `supported`   | `Window.destroy`                          | `routed`    |
| `Window`                    | `focus`                     | `supported`   | `Window.focus`                            | `routed`    |
| `Window`                    | `getBounds`                 | `supported`   | `Window.getBounds`                        | `routed`    |
| `Window`                    | `getById`                   | `supported`   | `Window.getById`                          | `routed`    |
| `Window`                    | `getChildren`               | `supported`   | `Window.getChildren`                      | `routed`    |
| `Window`                    | `getCurrent`                | `supported`   | `Window.getCurrent`                       | `routed`    |
| `Window`                    | `getParent`                 | `supported`   | `Window.getParent`                        | `routed`    |
| `Window`                    | `getState`                  | `supported`   | `Window.getState`                         | `routed`    |
| `Window`                    | `hide`                      | `supported`   | `Window.hide`                             | `routed`    |
| `Window`                    | `list`                      | `supported`   | `Window.list`                             | `routed`    |
| `Window`                    | `maximize`                  | `supported`   | `Window.maximize`                         | `routed`    |
| `Window`                    | `minimize`                  | `supported`   | `Window.minimize`                         | `routed`    |
| `Window`                    | `requestAttention`          | `supported`   | `Window.requestAttention`                 | `routed`    |
| `Window`                    | `restore`                   | `supported`   | `Window.restore`                          | `routed`    |
| `Window`                    | `setAlwaysOnTop`            | `supported`   | `Window.setAlwaysOnTop`                   | `routed`    |
| `Window`                    | `setBounds`                 | `supported`   | `Window.setBounds`                        | `routed`    |
| `Window`                    | `setDecorations`            | `supported`   | `Window.setDecorations`                   | `routed`    |
| `Window`                    | `setFullscreen`             | `supported`   | `Window.setFullscreen`                    | `routed`    |
| `Window`                    | `setProgress`               | `supported`   | `Window.setProgress`                      | `routed`    |
| `Window`                    | `setResizable`              | `supported`   | `Window.setResizable`                     | `routed`    |
| `Window`                    | `setSkipTaskbar`            | `partial`     | `Window.setSkipTaskbar`                   | `routed`    |
| `Window`                    | `setTitle`                  | `supported`   | `Window.setTitle`                         | `routed`    |
| `Window`                    | `setTrafficLights`          | `partial`     | `Window.setTrafficLights`                 | `routed`    |
| `Window`                    | `setVibrancy`               | `partial`     | `Window.setVibrancy`                      | `routed`    |
| `Window`                    | `show`                      | `supported`   | `Window.show`                             | `routed`    |
| `Window`                    | `subscribeEvents`           | `supported`   | `Window.Event`                            | `routed`    |
| `WorkspaceIndex`            | `close`                     | `supported`   | `WorkspaceIndex.close`                    | `routed`    |
| `WorkspaceIndex`            | `isSupported`               | `supported`   | `WorkspaceIndex.isSupported`              | `routed`    |
| `WorkspaceIndex`            | `open`                      | `supported`   | `WorkspaceIndex.open`                     | `routed`    |
| `WorkspaceIndex`            | `refresh`                   | `supported`   | `WorkspaceIndex.refresh`                  | `routed`    |
